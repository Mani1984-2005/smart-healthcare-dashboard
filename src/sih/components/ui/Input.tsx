import { InputHTMLAttributes } from "react";

type InputProps = InputHTMLAttributes<HTMLInputElement> & { label?: string; error?: string; hint?: string };
export default function Input({ className = "", error, hint, id, label, ...props }: InputProps) { const describedBy = error ? `${id}-error` : hint ? `${id}-hint` : undefined; return <label className="block text-sm font-medium text-slate-700 dark:text-slate-200">{label}{<input id={id} aria-invalid={Boolean(error)} aria-describedby={describedBy} className={`mt-2 block min-h-10 w-full rounded-lg border bg-white px-3 py-2 text-sm text-slate-900 transition placeholder:text-slate-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-600 focus-visible:ring-offset-1 dark:bg-slate-950 dark:text-slate-100 ${error ? "border-rose-600" : "border-slate-300 dark:border-slate-700"} ${className}`} {...props} />}{error ? <span id={`${id}-error`} className="mt-1 block text-xs text-rose-700 dark:text-rose-300">{error}</span> : hint ? <span id={`${id}-hint`} className="mt-1 block text-xs font-normal text-slate-500 dark:text-slate-400">{hint}</span> : null}</label>; }
