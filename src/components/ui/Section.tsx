import { HTMLAttributes, ReactNode } from "react";
type SectionProps = HTMLAttributes<HTMLElement> & { children: ReactNode; title?: string; description?: string; action?: ReactNode };
export default function Section({ action, children, className = "", description, title, ...props }: SectionProps) { return <section className={`rounded-xl border border-slate-200 bg-white p-6 shadow-card dark:border-slate-800 dark:bg-slate-950 ${className}`} {...props}>{(title || description || action) && <div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div>{title && <h2 className="text-base font-semibold text-slate-900 dark:text-slate-100">{title}</h2>}{description && <p className="mt-1 text-sm text-slate-600 dark:text-slate-400">{description}</p>}</div>{action}</div>}{children}</section>; }
